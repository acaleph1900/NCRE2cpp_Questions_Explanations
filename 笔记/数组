#include <iostream>
#include <string>
using namespace std;
int main()
{
	string a[5] = { "A", "B", "C", "D", "E" };
	string b[] = { "一", "二", "三", "四", "五" };
	//大括号，用逗号分隔
	//中括号内的数字表示数组的大小，可以省略

	int c[5] = { 1, 2, 3, 4, 5 };
	//整型数组

	cout << a[0] << endl; //输出第一个元素
	//从零开始算

	for (int i = 0; i <=4; i++) 
	{
		cout<< i  << a[i] << endl;
	}
	//循环输出数组元素

	return 0;
}
